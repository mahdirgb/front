import React from 'react';

const person = (props)=>{
//     return <p>I 'm a mahdi mousavi your age : {Math.floor(Math.random()*30)}</p>
//    
  return <span>{props.name ,props.age}</span>
}

export default person;